/*
use robot_code_gen::generate_robot_code;

fn main() {
    let crate_name = env!("CARGO_PKG_NAME").to_string();
    let out_file_path = format!("./tests/common/{crate_name}_robot.rs");

    generate_robot_code("./src/msg.rs", &out_file_path, Some(crate_name));
}
*/

fn main() {
    
}